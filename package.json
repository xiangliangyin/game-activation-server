{
  "name": "activation-api",
  "version": "1.0.0",
  "type": "commonjs",
  "scripts": {
    "dev": "vercel dev",
    "deploy": "vercel --prod",
    "import": "node scripts/import-codes.js",
    "verify": "node scripts/verify.js",
    "start": "vercel dev"
  },
  "dependencies": {
    "@neondatabase/serverless": "^0.9.0",
    "dotenv": "^16.3.1"
  },
  "devDependencies": {
    "@vercel/node": "^3.0.0"
  },
  "engines": {
    "node": ">=18"
  }
}
