{
  "name": "activation-code-api",
  "version": "1.0.0",
  "description": "高性能激活码验证系统",
  "main": "api/activate.js",
  "scripts": {
    "dev": "vercel dev",
    "deploy": "vercel --prod",
    "import": "node import-codes.js",
    "status": "curl https://your-project.vercel.app/api/status"
  },
  "dependencies": {
    "@neondatabase/serverless": "^0.9.0"  // 替换 @vercel/postgres
  },
  "devDependencies": {
    "vercel": "^32.0.0"  // 更新到最新版本
  },
  "engines": {
    "node": ">=18.x"
  }
}
