{
  "name": "activation-system",
  "version": "1.0.0",
  "scripts": {
    "dev": "vercel dev",
    "deploy": "vercel --prod",
    "import": "node import-codes.js",
    "verify": "node scripts/verify.js",
    "build": "node import-codes.js"
  },
  "dependencies": {
    "@neondatabase/serverless": "^0.9.0"
  },
  "engines": {
    "node": ">=18"
  }
}
